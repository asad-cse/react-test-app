{
  "name": "my-react-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "scripts": {
    "start": "npx serve -s build",
    "build": "npx esbuild src/index.jsx --bundle --outfile=build/bundle.js --define:process.env.NODE_ENV='\"production\"' --loader:.jsx=jsx --minify",
    "test": "echo \"No tests configured\""
  }
}
